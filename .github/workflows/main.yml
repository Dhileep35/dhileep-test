name: Demo

on:
  workflow_dispatch:

jobs:
  job-name:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Composite Action
        uses: ./.github/actions
