name: Demo

on:
  workflow_dispatch: 

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run Composite Action
        uses: ./.github/actions/my-action
