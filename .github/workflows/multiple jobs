name: create multiple jobs

on:
 push:
  branches: [main]

jobs:
 job-name:
  runs-on: ubuntu-latest

  steps:
   - name: Printing
     run: echo "Hello World"

jobs:
 job-name1:
  runs-on: ubuntu-latest

  steps:
   - name: print
     run: echo "$welcome to Github"
