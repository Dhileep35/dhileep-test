name: Parallel Example

on: 
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build"

  Test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test"

  Deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy"
