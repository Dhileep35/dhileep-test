name: matrix Example

on:
 workflow_dispatch:

jobs:
  job-name:
    runs-on: ${{ matrix.os }}
    strategy:
       matrix:
         java: [17,21]
         os: [ubuntu-latest, windows-latest]
    steps:
     - run: echo "java ${{ matrix.java }}"
  
         
